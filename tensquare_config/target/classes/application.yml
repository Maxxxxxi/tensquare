server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/singleR3/tensquare_test.git
  rabbitmq:
    host: 192.168.216.133
management:
  endpoints:
    web:
      exposure:
        exclude: bus-refresh